import React from 'react';
import Hero from '../../components/Hero';
import Catalog from '../../components/Catalog';

const Home = (props) => {
  return (
    <div>
      <Hero />

      <Catalog/>
    </div>)
};

export default Home;
